# improvements need to do

1. assign per post column & functionality from post types, only current enable post meta will be shown which created either using custom way or acf or cmb2 plugin. 
2. remove post title and content disable thing
3. remove custom value column from generate posts
4. delete users by role type and filter option like in post type
5. current available user meta will be shown whichbasically generate either in custom way or acf or using cmb2 plugin. 